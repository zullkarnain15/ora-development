org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# Required by AGP 9 plugins whose Kotlin sources are compiled by Android.
android.builtInKotlin=true
# Avoid Kotlin incremental-cache path failures when the pub cache and project
# are on different Windows drive roots (for example C: and D:).
kotlin.incremental=false
